const shuffle = require("../src/shuffle");

describe("shuffle should...", () => {
  // CODE HERE
  test("return a number if i won the fight game", () => {
    expect(math.abs(1)).toBe(1);
  });

  test("return a positive if i loss the fight game", () => {
    expect(math.abs(-1)).toBe(1);
  });
});
